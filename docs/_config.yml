# GitHub Pages 站点配置（静态文件直接托管，无需 Jekyll 渲染）
title: Linkora · 灵桥
description: 把 AI 分身接进钉钉、飞书和企业微信 —— 读得懂公司文档，会自己动手办事。

# 禁用 Jekyll 处理（纯静态模式）
skip_config_check: true
exclude:
  - "*.mermaid"
  - "audit/**/*"
  - ".git"
  - "*.yaml"
  - "*.yml"
  
# 指定输出目录
destination: _site
