# agent.py 瘦身拆分完成（2026-07-24 22:50）

## 目标
把 1956 行的 agent.py 拆成多个子模块，每个方法保留 thin wrapper，确保：
- 测试 monkey-patch 路径不变（Pytest mock.patch 路径兼容）
- 零行为变化（全量测试通过数不变）
- 模块边界清晰

## 拆分结果

| 文件 | 行数 | 内容 |
|------|------|------|
| `agent.py` (改) | **1144** (-812) | thin wrapper + process_message 主循环 |
| `history.py` (新) | 203 | `_apply_history_tiering` / `_read_cached_summary` / `_maybe_schedule_summary` / `_estimate_tokens` / `_truncate_long_message` / `_RE_CHINESE` |
| `style.py` (新) | 371 | `_build_system_prompt_core` / `_build_system_prompt` / `_get_style_prompt` / `_enforce_brevity` / `_get_embedding_client` / `_embed_message` / `_cosine` / `_is_document_query` / `_extract_relevant_snippets` / `_retrieve_relevant_knowledge` / 锚点常量 / 阈值 |
| `router.py` (新) | 494 | `_BASE_TOOL_NAMES` / `_FALLBACK_TOOL_NAMES` / `_TOOL_RESULT_TTL` / `_check_stale_tool_results` / `_is_tool_for_platform` / `_filter_schemas_by_platform` / `_keyword_match_tool_names` / `_semantic_tool_matches` / `_filter_tools_by_intent` / `_resolve_routing_mode` / `_merge_proactive_action_tools` / `_select_tools` |
| `system_prompt.py` (新) | 112 | 系统提示词构建/加载 |

agent.py 共 **减 41.5%**，**812 行** → 子模块。

## 关键决策

1. **保留方法签名 + 1 行委托**：原方法 `agent._foo(args)` 仍可调用，子模块 `agent._router.foo(agent, args)` 实现
2. **模块级常量 re-export**：`from src.llm.style import _RAG_CONFIDENT_SIMILARITY, _RE_HAS_TEXT` 等，供 `_build_user_message` 主体继续引用（避免改 `_build_user_message`）
3. **类属性 → 模块级常量**：`_BASE_TOOL_NAMES` 类属性删除，改为 `router.BASE_TOOL_NAMES`（模块级），子模块内用模块级常量；agent.py 主体不再访问这些属性
4. **`_select_tools` 整函数 thin wrapper**：是大函数，wrap 后整段函数体删除（547-686 行）

## 测试

```
$ .venv/bin/python -m pytest tests/ -x --tb=short -q
2013 passed, 2 skipped, 1 warning in 281.18s (0:04:41)
```

与重构前完全一致：**零回归**。

## 提交

- `43c9f29` on `refactor/split-agent-py`
- 已 push 到 `origin/refactor/split-agent-py`

## 待用户手动重启

按用户偏好**不自动重启**，请用户执行：

```bash
pkill -f "main.py --dev" ; sleep 2
cd /Users/ring0/Documents/dingtalk && .venv/bin/python main.py --dev
```

服务重启后新代码生效（hot-reload 不重启进程会沿用旧 agent.py）。

## 后续待办

- DWS Adapter dry_run 竞态重构（13 处临时写入替换 `force_no_dry_run`）— 已完成但未提交
- 多模态理解缺失（OCR 仅文本，无视觉输入）
- 路由追踪页面 `routetrace` 工作区文件未提交
- `reload_config` 中需清 `_style_prompt_cache`
- `refactor/split-agent-py` 合并到 master（如确认无回归）