# 文档核对与修订报告（2026-07-30）

## 背景

需求：全面梳理项目，保证文档完全贴合项目实际。

方法：以 Explore 子代理构建代码真值地图（src/ 实际结构、配置 schema、`config.yaml.example`、`pyproject.toml`、Web 路由、前端页面），再逐篇核对 8 篇对外文档（README、architecture、design、configuration、web-api、frontend_module_map、DEV_GUIDE、deployment）以及 rag/memory/faq，对每一条"事实性声明"与实现比对。

## 真值基线（核对依据）

| 事实 | 真实值 | 来源 |
|---|---|---|
| 内置工具数 | **27** | `src/tools/registry.py::BUILTIN_TOOL_MANIFEST` |
| 意图数 | **33**（6 处置 + 7 行动 + 20 领域） | `src/intent.py::DEFAULT_INTENTS` |
| 默认数据库 | 钉钉 `./data/linkora.db`；飞书/企微仅显式配置 `storage.path` 才独立库 | `src/config.py::PlatformConfig` + `config.yaml.example` |
| Python 要求 | `requires-python = ">=3.9"`（推荐 3.13/3.14） | `pyproject.toml` |
| 真实入口 | `src/platform/lifecycle.py::main`（`main.py` 仅为兼容门面） | `src/platform/lifecycle.py` |
| Web 规模 | 约 30 个路由模块、150+ 端点（挂载 29 个 router） | `web/api.py` grep |
| 前端顶层页 | **15**（`ALLOWED_PAGES`） | `web/static/js/core/app.js` |
| Embedding 默认 | `enabled=False`，`model=BAAI/bge-small-zh-v1.5` | `src/config.py::EmbeddingConfig` |
| RAG 高级默认 | `rag_min_similarity=0.6`、`rag_max_results=1`、`rag_max_content_chars=800` | `src/config.py::LlmAdvancedConfig` |
| Web 认证 | 扁平字段 `auth_enabled/auth_username/auth_password`；空密码+开启即启动失败（fail-closed） | `src/config.py::WebConfig` |
| 关键文件行数 | poller 266 / rule_engine 407 / intent 890 / agent 1162 / decision_tracker 189 / sqlite_store 1162（已拆 `*_repo.py`）/ config 1029 / db_backup 282 / doc_sync_scheduler 260 | `wc -l` |

## 已修订的文件与问题

| 文件 | 问题 | 修订 |
|---|---|---|
| README.md | embedding 示例模型写成 `./data/models/bge-large-zh-v1.5`、未提 `enabled` | 改为默认 `BAAI/bge-small-zh-v1.5` 并加 `enabled` 说明 |
| README.md | `llm.advanced.rag_min_similarity: 0.3`、`rag_max_content_chars: 1200` 与代码默认不符 | 改为 `0.6` / `800`，并补 `rag_max_results: 1` |
| README.md | 项目树根目录写成 `dingtalk/` | 改为 `linkora/` |
| README.md | `intent.py … 27 意图` 与正文"33 意图"自相矛盾 | 改为"33 意图：13 通用 + 20 领域" |
| architecture.md | "独立数据库 `data/{platform_id}-ai.db`" 误导（钉钉实为 linkora.db） | 改为说明默认 linkora.db、飞书/企微显式独立库 |
| architecture.md | "按 `_active_platform_ctx` ContextVar 路由" 名字已变更 | 改为实际 `set_current_platform`/`get_current_platform` |
| architecture.md | "31 个 RESTful 路由端点" | 改为"约 30 个路由模块 · 150+ 端点" |
| architecture.md | "26 内置工具" | 改为 27 |
| design.md | 多处"26 个工具" + 工具表缺 `transfer_approval` | 全文改为 27，并在工具表补全 `transfer_approval`（第 18 项） |
| design.md | 大量 per-file 行数严重过时（poller 986→266、agent 1956→1162、sqlite_store 4512→1162、intent 528/827→890、config 855→1029 等） | 全部更新为实测算值，标注"约" |
| configuration.md | `platforms` 行把库名写成 `./data/<id>-ai.db`，与"默认 linkora.db"自相矛盾 | 澄清默认 linkora.db、飞书/企微显式独立库 |
| configuration.md | `llm.advanced` 缺 `rag_min_similarity` / `rag_max_content_chars` | 补入并标注默认 |
| web-api.md | 端点分组表含多个不存在的前缀（`/api/auth`、`/api/dashboard`、`/api/memory`、`/api/kb/{id}/chunks`、`/api/system-prompt`、`/api/contacts`、`/api/docs`、`/api/history`） | 重写为真实路由前缀（`/api/memories`、`/api/llm/prompt`、`/api/dingtalk-docs` 等） |
| web-api.md | 响应示例 `"tools_count": 26` | 改为 27 |
| frontend_module_map.md | "保持 14 页路由"、导航表漏 `logs` 页、`summary.totals` 写成端点 | 改为 15 页、补 `logs`、注明 `summary.totals` 是 `/summary` 的响应字段 |
| DEV_GUIDE.md | Python 最低版本写成 3.11+ | 改为 `>=3.9`（推荐 3.13/3.14） |
| DEV_GUIDE.md | embedding 示例用不存在的 `model_name`/`device` 键 | 改为真实 `model`/`provider`/`offline`/`enabled` |
| DEV_GUIDE.md | rules 示例用不存在的 `keyword_match`/`intent` 子段 | 改为真实 `blacklist`/`keywords`/`intent_filter` |
| DEV_GUIDE.md | web 示例用嵌套 `auth:`（应为扁平 `auth_enabled` 等） | 改为真实扁平字段并说明 fail-closed |
| DEV_GUIDE.md | `git clone … dingtalk` / `cd dingtalk`；测试文件数"110+" | 改为 `linkora`；约 190 个 |
| deployment.md | Python 版本写成 3.13+ | 改为 `>=3.9`（推荐 3.13/3.14） |
| rag.md / memory.md / faq.md / tools.md / intent-model.md | 核对后基本准确 | 未改动（tools.md 27 项、faq 冷却默认 60 等均与实际一致） |

## 本轮未逐一核验的文档

以下为**历史/设计/审计报告**，描述特定时点或前瞻方案，未逐条复核其内部计数（可能随演进漂移）：
`project_audit_2026-07-14.md`、`multi-platform-audit-report.md`、`intent-audit-20260726.md`、`memory-classifier-audit-20260726.md`、`../main_refactor_design.md`、`../phase0_hardening_design.md`、`../phase2_citation_confidence_design.md`、`../cost_quality_dashboard_design.md`、`../system_design.md`。

建议：此类文档若引用"当前"数字（工具数/意图数/端点数/行数），后续随代码变更时同步更新，或改写为不依赖易变计数的表述。

## 结论

对外文档中与代码实现直接冲突的**事实性错误已全部修订**（工具 27、意图 33、默认库 linkora.db、Python ≥3.9、真实路由前缀、真实配置键、过时行数）。修订后 README/architecture/design/configuration/web-api/frontend_module_map/DEV_GUIDE/deployment 与 `src/` 实际行为一致。
