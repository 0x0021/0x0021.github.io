# 安全政策 · Security Policy

本政策适用于 **灵桥 · Linkora** 仓库。感谢你帮助守护项目与用户数据的安全。

## 受支持的版本

| 版本 | 是否接受安全更新 |
| --- | --- |
| `main` 分支最新提交 | ✅ 是 |
| 历史 Release | ❌ 否（请升级到最新 `main` 或最新 Release） |

安全修复会随常规发布合入 `main`，并纳入下一个 Release。

## 报告漏洞

> ⚠️ **请勿通过公开的 Issue、Pull Request、Discussion 或任何公开渠道报告安全漏洞。**

请使用 GitHub 内置的**私有漏洞上报（Private Vulnerability Reporting）**：

1. 打开本仓库的 **Security（安全）** 标签页；
2. 进入 **Vulnerability reporting（漏洞上报）**；
3. 填写表单提交，报告仅仓库维护者可见，不会公开。

如果你在仓库设置中未看到上报入口，可改为向维护者发起 **私有安全 Advisory**（同样通过 Security 标签页的 **Advisories → Report a vulnerability**）。

### 响应时限

- **72 小时内**：确认收到报告并分配处理人；
- 确认有效后，与报告人协商修复方案与**协同披露（coordinated disclosure）**时间线；
- 修复合并并发布后，默认在 Advisory 中致谢报告人（如你希望匿名请注明）。

### 报告中请尽量包含

- 漏洞类型与受影响组件 / 文件路径；
- 复现步骤（最小可复现用例最佳）；
- 潜在影响与攻击前置条件；
- 如有，附上缓解建议或补丁草稿。

## 安全设计要点（供审计）

本项目在设计层面已内置若干安全基线，欢迎在审计时重点验证：

- **密钥不入库**：LLM API Key、钉钉 / 飞书 / 企微 AppSecret、数据库路径等仅存在于本地 `config.yaml`，该文件已加入 `.gitignore`，**绝不进入版本控制**。
- **CI 密钥扫描**：每次推送运行 `gitleaks` 扫描提交历史与增量，命中即阻断。
- **依赖锁定与漏洞扫描**：依赖版本锁定于 `requirements.lock`，CI 中定期运行 `pip-audit` 扫描已知漏洞。
- **多平台数据物理隔离**：钉钉 / 飞书 / 企业微信各自独立适配器、独立 SQLite 库、独立轮询器，数据互不可见、互不越权。
- **输入校验**：用户传入的 URL 抓取 / 导航须经 SSRF 校验，禁止直连未校验地址。
- **最小权限运行**：以普通用户身份运行 worker / web 进程，避免以 root 常驻。

## 不建议公开讨论的安全话题

- 具体密钥、token、证书的明文或截图；
- 可在生产环境直接利用的 0day / 未修复漏洞的复现细节（请走私有上报通道）；
- 第三方平台（钉钉 / 飞书 / 企微）的绕过性技巧。

我们会在修复落地并发布后，再考虑在文档 / 博客中复盘技术细节。
